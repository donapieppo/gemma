<!DOCTYPE html>
<html lang="it">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="Gestione Multiutente MAteriale di consumo. Università di Bologna.">
    <meta name="author" content="Dipartimento di Matematica - Unibo - Pietro Donatini">
    <title>GE.M.MA - GEstione Multiutente MAteriale di consumo</title>

    <%= javascript_include_tag "application" %>
    <%= stylesheet_link_tag "fa.all.min", "application" %>

    <%= csrf_meta_tag %>
    <%= csp_meta_tag %>

    <script>
      $(document).ready(function() {
        $('.upn').click(function(e) {
          $('#recipient', window.parent.document).val(this.id);
          e.preventDefault();
        });
      })
    </script>

  </head>
  <body>

    <!-- FIXME cancella -->
    <div style="margin: 10px;">
      <%= raise "NON DEVO ESSERE" %>  
      <%= yield %>
    </div>

  </body>
</html>

