<h1>Risultati dell'operazione di scarico</h1>

<% if @batch_unloads.errors.any? %>
  <div class="alert alert-danger">
    <% @batch_unloads.errors.each do |e| %>  
      <% if e.respond_to?(:full_messages) %>  
        <p><%= e.full_messages %></p>
      <% else %>
        <p><%= e %></p>
      <% end %>
    <% end %>
  </div>
<% else %>
  Operazione di scarico avvenuta correttamente per i seguenti utenti:
  <ul>
    <% @batch_unloads.ok.each do |r| %>  
      <li><%= r %></li>
    <% end %>
  </ul>
  
  <%= link_to "Mostra i movimenti di \"#{@thing}\"", thing_moves_path(@thing) %> &nbsp; - &nbsp; 
  <%= link_to "Esegui un nuovo scarico multiplo sullo stesso oggetto", new_thing_unload_path(@thing, batch: 'y') %>  
<% end %>

