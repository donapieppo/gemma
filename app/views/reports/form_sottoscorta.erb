<%= report_layout title: "Articoli sottoscorta" do %>  
  <%= simple_form_for :report, url: sottoscorta_reports_path, data: { turbo: false } do |f| %>
    <%= render Report::FormatComponent.new(f) %>  
    <%= f.submit "Scarica", input_html: { data: { disable_with: false }} %>
  <% end %>
<% end %>
