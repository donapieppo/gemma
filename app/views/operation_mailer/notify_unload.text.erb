Gentile <%= @user %>,

  in data odierna Lei ha ritirato 

*** numero <%= @unload.number.to_i.abs %> di "<%= @unload.thing %>" ***
<%= @unload.price if @organization.pricing  %>  

attraverso il programma di richiesta materiale di consumo - cancelleria 
presso la struttura "<%= @organization.description %>"

L'amministrazione Le ha inviato questa mail come promemoria.

  Con i migliori saluti

	Amministrazione 
	<%= @organization.description %>

