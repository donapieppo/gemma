<h1>DDT/Fatture
  <%= @supplier ? "di #{@supplier}" : '' %>  
  <% if @titolo_ricerca %> 
    <small>(<%= @titolo_ricerca %>)</small>
  <% end %>
</h1>

<div class="row">
  <div class="col-md-3">
    <%= render Ddt::SearchMenuComponent.new(current_organization) %>
  </div>

  <div class="col-md-9">
    <p class="centered_action"><%= link_to_new 'Registra nuovo ddt/fattura', suppliers_path(for: 'ddt') %></p>
    <%= render Ddt::TableComponent.new(@ddts, current_user) %>  
  </div>
</div>

