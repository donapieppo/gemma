<div>
  <% if !(@operation.is_unload? && @operation.number >= -1) %>
    <%= link_to_edit "", [:edit, @operation] %>
  <% end %>
  <%= link_to_delete "", @operation %>
  <% if @operation.is_unload? && !@operation.is_booking? && @operation.recipient %>
    <a href="#" 
       title="ricevuta" 
       onClick="window.open('<%= signing_unload_path(@operation.id) %>', 'ricevuta', 'width=700,height=500,scrollbars=no');">
       <%= dm_icon("file") %>
    </a>
  <% end %>
</div>
